# Задачи для агентов (общая нагрузка PoC)

Три **независимые** задачи → каждая в своём worktree/ветке, исполняется параллельно.
Независимость нужна, чтобы проверить отсутствие конфликтов и чистый merge.

## task-1: multiply (ветка `task/multiply`)
Добавить в `src/calc.js` функцию `multiply(a, b)` и тест в `tests/calc.test.js`.
Тесты должны проходить (`npm test`).

## task-2: docs (ветка `task/docs`)
В `README.md` sample-repo добавить раздел «API» с описанием всех экспортируемых функций.
Код не трогать.

## task-3: validate (ветка `task/validate`)
Рефактор `add`/`subtract`: бросать `TypeError`, если аргумент не число. Добавить тест на ошибку.
Тесты должны проходить.

---

Промпт агенту (шаблон, подставляется в PoC-скриптах):

> Ты в git worktree. Прочитай задачу `<TASK>` ниже и выполни её полностью: внеси правки,
> запусти `npm test`, добейся зелёных тестов. Не выходи за пределы текущего рабочего каталога.
> Не задавай уточняющих вопросов — действуй автономно.
> Задача: `<TASK_BODY>`
