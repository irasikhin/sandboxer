# Матрица оценки PoC

Шкала 1–5 (5 = лучше). Заполняется по итогам Фазы 2. Пока — шаблон с прочерками.

| Критерий | Native (`/sandbox` + worktree) | srt (bubblewrap) | Docker-per-agent (sbox) |
|---|---|---|---|
| Изоляция FS (запись только в worktree) | – | – | – |
| Изоляция сети (allowlist / контроль egress) | – | – | – |
| Защита хоста при автономном прогоне | – | – | – |
| Эргономика параллели (N агентов, per-worktree) | – | – | – |
| Изоляция веток (нет конфликтов правок) | – | – | – |
| Setup-стоимость / зависимости (на NixOS!) | – | – | – |
| Накладные расходы (старт на агента, лимиты CPU/RAM) | – | – | – |
| Diff/merge workflow (собрать диффы, слить ветки) | – | – | – |
| Зрелость / поддержка OSS | – | – | – |
| Секреты (проброс API-ключей без утечки) | – | – | – |
| Логи / observability на агента | – | – | – |
| **Σ** | – | – | – |

## Негативные тесты изоляции (pass = ограничение сработало)

| Тест | Native | srt | Docker |
|---|---|---|---|
| Запись вне worktree (напр. `$HOME/x`, `/etc/x`) — должна **падать** | – | – | – |
| Сеть к не-allowlisted хосту — должна **падать** (если включён allowlist) | – | – | – |
| Запись внутри worktree — должна **работать** | – | – | – |
| Сеть к разрешённому (api.anthropic.com) — должна **работать** | – | – | – |

## Заметки по кандидатам

### Native
-

### srt
- NixOS: bind `/nix/store`, профиль —

### Docker-per-agent (sbox)
-

## Гейт build-or-adopt

- **adopt (native)** — если native ± тонкая обёртка закрывает требования.
- **adopt + враппер** — если native мало, но srt/docker близки.
- **build** — если эргономики параллели/изоляции на NixOS не хватает ни у кого.

Решение → пишется в `../README.md` (decision-log).
